#include <iostream>


#include <map>
#include <stack>
#include <vector>
#include <string>
#include <stdio.h>
#include <sstream>

using namespace std;
using ii = pair<int, int>;
using vi = vector<int>;
using vs = vector<string>;
using vii = vector<ii>;
using ull = unsigned long long;
using ll = long long;
#define fu(type, start, end, step) for(type i = start; i < end; i += step)
#define getLineNoEmptyString(stream, string) while(getline(strem, string) && string.empty() {continue;}
bool testIsand(int i, int j)
{
    int n =5;
    if( 0 <= i < n && 0 <= j <= n) {
        
    }
    return true;
}

void tester()
{
    int i = 0;
    for ( ;i < 5; ++i) {
        cout << "testing i " << i << " " ;
    }
    int j = 5;
    testIsand(i,j);
}
//
//int main()
//
//{
//   int N;
//    cin >> N;
//    string str("this is a string for testing");
//    for (int i = 0; i < str.size(); ++i) {
//        cout << "this is the " << i << " char: " << str[i] << endl;
//    }
////    for (int i = 0; i < N; ++i) {
////        string s;
////        while(getline(cin, s) && s.empty()) {continue;}
//////        reverseStr(s) ;
////        cout << "Case #" << i+1 << ": " << s << endl;
////    }
//    
//    return 0;
//}
